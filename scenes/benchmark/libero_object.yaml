# LIBERO-Object benchmark — franka_panda fixed by the scene registry.
# Run with any LIBERO-Object-compatible rSkill:
#
#   openral benchmark scene --config scenes/benchmark/libero_object.yaml \
#                           --rskill rskills/<libero-object-rskill>
#
# Setup: CC=/usr/bin/gcc just sync --all-packages --group libero
#
# Same LiberoEnv backend + Franka panda as libero_spatial; only the suite
# (scene.id) and the eval horizon differ. The rSkill must declare
# evaluated_tasks=["libero_object"] — a LIBERO-Spatial-only
# checkpoint is refused here.

robot_id: franka_panda

scene:
  id: libero_object
  backend: mujoco
  observation_height: 256
  observation_width: 256

task:
  id: libero_object/0
  scene_id: libero_object
  instruction: ""        # env.task_description from the LIBERO suite overrides this
  success_key: is_success
  max_steps: 280         # OpenVLA run_libero_eval LIBERO-Object budget

n_episodes: 50           # OpenVLA protocol: 50 rollouts per task
seed: 42

metadata:
  paper: "https://arxiv.org/abs/2306.03310"
  honest_scope: >
    Task 0 of LIBERO-Object (Liu et al., 2306.03310). Official VLA-eval
    protocol (OpenVLA run_libero_eval): 10 tasks × 50 rollouts each (500
    total), max 280 steps/episode for the Object suite. Pass
    --task libero_object/N (N=0–9) to reproduce per-task numbers.
