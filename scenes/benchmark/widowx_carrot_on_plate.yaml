# SimplerEnv WidowX — carrot-on-plate task, SAPIEN/ManiSkill3 backend.
# Run: openral benchmark scene --config scenes/benchmark/widowx_carrot_on_plate.yaml \
#                              --rskill rskill://rskills/rldx1-ft-simpler-widowx-nf4
# Setup: just sync --all-packages --group simpler-env

robot_id: widowx

scene:
  id: simpler_env
  backend: mujoco            # SAPIEN under the hood
  observation_height: 480
  observation_width: 640
  cameras: ["top"]

task:
  id: simpler_env/widowx_carrot_on_plate
  scene_id: simpler_env
  instruction: "put the carrot on the plate"
  success_key: is_success
  max_steps: 60          # official SimplerEnv WidowX carrot horizon (MS3 TimeLimit)

n_episodes: 24           # SimplerEnv WidowX protocol: 24 trials per task
seed: 0

metadata:
  paper: "https://arxiv.org/abs/2405.05941"
  honest_scope: >
    SimplerEnv WidowX+Bridge carrot-on-plate (Li et al., 2405.05941).
    Official protocol: 24 trials per task, 60-step horizon (MS3 canonical
    max_episode_steps for the carrot/spoon/cube bridge tasks). SimplerEnv
    reports per-task visual-matching success rates.
