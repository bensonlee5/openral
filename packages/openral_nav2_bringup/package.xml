<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>openral_nav2_bringup</name>
  <version>0.1.0</version>
  <description>
    Bringup wrapper for upstream Nav2 as the second OpenRAL
    Reasoner-managed background service (after slam_toolbox). Ships a
    minimal Nav2 parameter set tuned for the panda_mobile holonomic
    base + a stand-alone launch file the deploy CLI includes when the
    robot manifest declares a wrapped-action navigate rSkill. The Nav2
    lifecycle nodes (bt_navigator, controller_server, planner_server,
    behavior_server, smoother_server, velocity_smoother) come up via
    Nav2's own lifecycle_manager (use_lifecycle_manager: true is the
    Nav2 convention — Nav2's bringup_launch.py wires it in-package).
  </description>
  <maintainer email="contact@openral.dev">OpenRAL Contributors</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>ament_cmake_python</buildtool_depend>

  <!-- The upstream Nav2 stack is the workhorse this package wraps.
       Declared as an exec_depend so `rosdep install` picks it up but it
       isn't required to BUILD this bringup wrapper. -->
  <exec_depend>nav2_bringup</exec_depend>
  <exec_depend>nav2_msgs</exec_depend>
  <exec_depend>rclpy</exec_depend>
  <exec_depend>lifecycle_msgs</exec_depend>

  <test_depend>ament_copyright</test_depend>
  <test_depend>ament_cmake_pytest</test_depend>
  <test_depend>python3-pytest</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
  </export>
</package>
